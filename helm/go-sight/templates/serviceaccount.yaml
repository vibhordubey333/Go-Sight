{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "go-sight.serviceAccountName" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "go-sight.labels" . | nindent 4 }}
{{- end }}
